{
  "name": "vuito-monorepo",
  "private": true,
  "workspaces": [
    "./packages/*"
  ],
  "engines": {
    "node": ">=12",
    "npm": "7.x"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test"
  }
}