{
  "name": "vuito-monorepo",
  "private": true,
  "workspaces": ["./packages/*"],
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test"
  }
}
